version: 2

before:
  hooks:
    - go mod tidy
    - go mod verify

builds:
  - id: grsync
    binary: grsync
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.Commit}}
      - -X main.date={{.Date}}
    flags:
      - -trimpath

archives:
  - id: grsync
    format: tar.gz
    name_template: >-
      {{ .ProjectName }}_
      {{- .Version }}_
      {{- .Os }}_
      {{- .Arch }}
    format_overrides:
      - goos: windows
        format: zip
    files:
      - README.md
      - LICENSE

checksum:
  name_template: 'checksums.txt'
  algorithm: sha256

changelog:
  sort: asc
  use: github
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^ci:'
      - '^chore:'
      - '^build\(deps\):'
  groups:
    - title: Features
      regexp: '^feat:'
      order: 0
    - title: Bug Fixes
      regexp: '^fix:'
      order: 1
    - title: Others
      order: 999

release:
  github:
    owner: mdeous
    name: grsync
  draft: false
  prerelease: auto
  mode: replace
  header: |
    ## Release {{ .Tag }}

    Download the appropriate binary for your platform below.
  footer: |
    ## Installation

    Download the binary for your system and extract it:

    ```bash
    # Linux/macOS
    tar xzf grsync_*.tar.gz

    # Windows
    unzip grsync_*.zip
    ```

    Then move the binary to a location in your PATH.

snapshot:
  name_template: '{{ .Version }}-next'

sboms:
  - artifacts: archive
